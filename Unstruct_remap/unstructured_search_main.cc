#include <stdio.h>
#include "unstructured_search.h"

int main () {
    int success;
    success = test_unstructured_hash_remap();
    printf("success: %i\n", success);
    
    uint **solutions;
    //solutions = unstructured_hash_remap ("length.x3d.00001", "example.x3d.00001", 0.1);
    
    
}
